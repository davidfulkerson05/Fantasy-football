// Bump this to 1499 once the founding-price window is over — nothing else
// needs to change, /api/checkout reads from here.
export const FOUNDING_PRICE_CENTS = 999;
export const PRICE_LABEL = "$9.99";
export const CURRENCY = "usd";
